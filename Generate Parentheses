class Solution:
    def generateParenthesis(self, n: int):
        res = []
        def dfs(s, o, c):
            if o == n and c == n:
                res.append(s)
                return
            if o < n:
                dfs(s + '(', o + 1, c)
            if c < o:
                dfs(s + ')', o, c + 1) 
        dfs("", 0, 0)
        return res
